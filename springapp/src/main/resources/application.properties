# Use a much longer secret key (at least 64 characters)
examly.app.jwtSecret=ThisIsAVeryLongAndSecureSecretKeyThatIsAtLeast64CharactersLongForHS512Algorithm123!
examly.app.jwtExpirationMs=86400000

# Swagger Configuration
enable.swagger.plugin=true
spring.mvc.pathmatch.matching-strategy=ant-path-matcher

# Active profile (change as needed)
spring.profiles.active=dev